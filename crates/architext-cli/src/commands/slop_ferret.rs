//! `slop-ferret` command stub — full implementation in Task 4.

use std::path::Path;

pub fn run(_target: &Path, _plan: &str, _discharge: &str, _findings: &str) {
    eprintln!("slop-ferret: not yet implemented");
    std::process::exit(2);
}
