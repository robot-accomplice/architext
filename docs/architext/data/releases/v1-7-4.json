{
  "id": "v1-7-4",
  "version": "1.7.4",
  "name": "Architext 1.7.4 Self-Contained Native Serve",
  "status": "completed",
  "posture": "shipped",
  "summary": "Patch release fixing `architext serve` on a pure off-npm install. The viewer dist is now EMBEDDED into the native binary (rust-embed in architext-serve), so a binary installed by the curl installer or `architext update` is self-contained — it no longer depends on a co-located <exe_dir>/dist that the GitHub-release download lacked. Latent since the 1.7.2 native-install feature; surfaced when serving from a binary after leaving npm. On-disk dist (dev / ARCHITEXT_VIEWER_DIST override / npm co-located) still wins; the release pipeline builds the viewer before compiling the CLI so the embed is populated.",
  "targetDate": "2026-06-22",
  "releasedAt": "2026-06-22T07:26:48.000Z",
  "lastUpdated": "2026-06-22T07:30:00.000Z",
  "updateSource": "Shipped to npm 2026-06-22 (1.7.4 launcher + all five per-platform binaries, dist-tag latest=1.7.4; publish.yml fully green incl. the new build-viewer job, roboticus-site dispatch, and the published-CLI smoke). DEFINITIVELY verified against the SHIPPED artifact: downloaded the published darwin-arm64 binary (16.3 MB) and ran its `serve` from a bare temp dir with no co-located dist — index.html 200 and the 2.46 MB wasm 200, served from the embedded viewer, no 'assets missing'. The user's exact off-npm scenario (native binary, serve) now works.",
  "scope": {
    "required": [
      {
        "id": "embed-viewer-in-binary",
        "title": "Embed the viewer into the native binary (off-npm serve fix)",
        "kind": "bug-fix",
        "summary": "RCA: serve resolved the viewer from <exe_dir>/dist, which the per-platform npm packages co-locate but the standalone GitHub-release binary (curl installer / architext update) does not include, so a pure off-npm install printed 'Package viewer assets are missing' on serve. Fix: rust-embed bakes crates/architext-viewer/dist into architext-serve; the static handler serves on-disk first (override / npm / dev) then falls back to embedded, and the CLI's missing-assets guard passes when the viewer is embedded. release-binaries.yml and publish.yml build the viewer dist and stage it before the CLI compiles (rust-embed embeds at compile time), with a build.rs guard so non-viewer builds still compile.",
        "evidence": ["pr-244", "serve-embed-verify"],
        "status": "complete",
        "owner": "maintainer",
        "priority": "high",
        "workstreamId": "off-npm-serve",
        "dependsOn": []
      }
    ],
    "planned": [],
    "stretch": [],
    "deferred": [
      {
        "id": "drop-redundant-npm-co-location",
        "title": "Stop co-locating the dist in the npm packages (now embedded)",
        "kind": "chore",
        "summary": "With the viewer embedded in the binary, the per-platform npm packages' co-located <exe_dir>/dist is redundant (~2.6 MB per package). Deferral reason: out of scope for the serve hotfix; removing it is a packaging optimization to land once 1.7.4 is verified in the wild, and the on-disk copy is harmless (it simply wins over the identical embedded copy).",
        "status": "deferred",
        "owner": "maintainer",
        "priority": "low",
        "dependsOn": ["embed-viewer-in-binary"]
      }
    ],
    "outOfScope": []
  },
  "workstreams": [
    {
      "id": "off-npm-serve",
      "name": "Self-contained native serve",
      "owner": "maintainer",
      "status": "complete",
      "posture": "on-track",
      "summary": "Make the native binary serve the viewer with no co-located files, so the off-npm install/update path works end to end.",
      "progress": 100,
      "itemIds": ["embed-viewer-in-binary"],
      "evidence": ["pr-244", "serve-embed-verify"]
    }
  ],
  "blockers": [],
  "milestones": [],
  "dependencies": [],
  "evidence": [
    {
      "id": "pr-244",
      "label": "PR embedding the viewer in the native binary + pipeline",
      "href": "https://github.com/robot-accomplice/architext/pull/244",
      "kind": "document",
      "status": "complete"
    },
    {
      "id": "serve-embed-verify",
      "label": "Release binary serves the viewer with no on-disk dist (browser-verified)",
      "kind": "manual-check",
      "status": "complete"
    }
  ]
}
